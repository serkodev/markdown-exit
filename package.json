{
  "name": "@markdown-exit/monorepo",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest",
    "typecheck": "tsc --noEmit",
    "release": "pnpm lint && pnpm test --run && bumpp -r && pnpm -r publish",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@types/node": "catalog:types",
    "bumpp": "catalog:",
    "eslint": "catalog:",
    "shiki": "catalog:tests",
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:tests"
  }
}
